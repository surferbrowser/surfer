
export default class Rect {
    x: number
    y: number
    width: number
    height: number
    constructor(width: number, height: number, x: number, y: number) {
        this.x = x
        this.y = y
        this.width = width
        this.height = height
    }
}